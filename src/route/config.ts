import {IRoute} from '../model/route'

export const routes: {routes: IRoute[]} = {
  routes: [
    { key: '0', path: '/login', page: 'Login' },
    { key: '1', path: '/welcome', page: 'Welcome' }
  ]
}